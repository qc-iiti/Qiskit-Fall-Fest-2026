# Public Supabase project URL and anon key are NOT used directly by the frontend
# (all writes go through the NestJS backend), but NEXT_PUBLIC_API_URL must point
# to your deployed backend.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Set automatically by the GitHub Actions workflow to "/<repo-name>".
# Leave empty for local development.
NEXT_PUBLIC_BASE_PATH=
